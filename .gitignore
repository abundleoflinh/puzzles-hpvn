node_modules/
dist/
.wrangler/
.env
.env.local
# Local wrangler dev secrets (EDITOR_PASSWORD etc.) — never commit.
.dev.vars
worker/.dev.vars
.DS_Store
*.log

# Local wrangler config carries the real KV namespace id — see wrangler.toml.example.
worker/wrangler.toml

# Catfishing scrape intermediates. scrape-raw.json is committed as the source of
# truth for build-catalog.mjs; scrape-progress.json is a per-run resume file.
data/scrape-progress.json
